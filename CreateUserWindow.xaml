<Window x:Class="bankk.CreateUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add User" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="First Name:" VerticalAlignment="Center" Margin="10"/>
        <TextBox x:Name="FirstNameTextBox" Grid.Row="0" Margin="120,10,10,10"/>

        <TextBlock Grid.Row="1" Text="Last Name:" VerticalAlignment="Center" Margin="10"/>
        <TextBox x:Name="LastNameTextBox" Grid.Row="1" Margin="120,10,10,10"/>

        <TextBlock Grid.Row="2" Text="Email:" VerticalAlignment="Center" Margin="10"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="2" Margin="120,10,10,10"/>

        <TextBlock Grid.Row="3" Text="Password:" VerticalAlignment="Center" Margin="10"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Margin="120,10,10,10"/>

        <Button Grid.Row="4" Content="Add User" HorizontalAlignment="Center" Margin="10" Click="AddUserButton_Click"/>

    </Grid>
</Window>
