<Window x:Class="bankk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bank Login" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 0 0 10">
            <TextBlock Text="Email:" VerticalAlignment="Center" Margin="0 0 5 0" />
            <TextBox x:Name="EmailTextBox" Width="279" Height="Auto" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 10">
            <TextBlock Text="Hasło:" VerticalAlignment="Center" Margin="0 0 5 0" />
            <PasswordBox x:Name="PasswordBox" Width="278" VerticalAlignment="Center" />
        </StackPanel>

        <Button Grid.Row="2" Content="Login" Click="LoginButton_Click" HorizontalAlignment="Left" Width="91" Margin="190,31,0,132" />
        <Button Content="Dodaj Klienta" HorizontalAlignment="Left" Click="AddUserButton_Click" Margin="72,31,0,132" Grid.Row="2" Width="91"/>

    </Grid>
</Window>
